console.log("David");
// here i show how can we take input from user.
 let name = prompt("Enter your name");
 console.log(name);
// this code not working because we can not take input from user in strict mode.
// using form we can take input then we can use it in our code.